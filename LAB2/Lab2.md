ssh 231b264@192.168.4.3    # remote ubuntu access

rishabh264                 # password of the remote ubuntu

passwd                     # path

cd 231b264                 # only cd is command

cd ..                      # previous directory

ls                         # list

ls -l

touch a1                   # only touch is command

cat a1                     # only cat is command

cat >a2                    # only cat > is command

cat >>a2                   # only cat >> is command 

rm a1                      # remove a1
 
mkdir a10                  # make directory

rmdir a10                  # remove directory

cp a2 a10                  # only cp is command means copy

mv a2 a4                   # only mv is command means move 

man ls                     # manual for ls

# neo vim editor 

vi filename -> edit esc 
w -> save
q -> exit
w! -> with out save outs

gcc new.c                  # run file

./a.out                    # single output

gcc new.c -o new           # run multi files

./new                      # multi output